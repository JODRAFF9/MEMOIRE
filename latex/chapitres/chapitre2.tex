\section{Données et méthodologie}
\label{sec:methodo}

Cette section présente l'architecture méthodologique de la recherche. La mesure
de la pauvreté multidimensionnelle des enfants repose sur l'approche MODA
(Multiple Overlapping Deprivation Analysis) de l'UNICEF, qui analyse les
privations superposées en distinguant les groupes d'âge. Cette mesure constitue
la variable de résultat de la stratégie d'identification causale fondée sur
la combinaison du score de propension (PSM) et de la double différence (DD),
mise en œuvre sur les deux vagues de l'Enquête Harmonisée sur les Conditions
de Vie des Ménages (EHCVM I et II).

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Données : les enquêtes EHCVM I et II}
% ─────────────────────────────────────────────────────────────────────────────

\subsubsection{Présentation des enquêtes}

Les Enquêtes Harmonisées sur les Conditions de Vie des Ménages (EHCVM) sont
réalisées dans le cadre du programme statistique de l'UEMOA. L'EHCVM
I (2018-2019) est la première vague nationale au Sénégal \parencite{ansd2019},
reposant sur un plan de sondage stratifié à deux degrés (régions x milieux
comme strates, zones de dénombrements comme unités primaires). L'EHCVM
II (2021-2022) reproduit le même protocole \parencite{ansd2022}, permettant
la constitution d'un pseudo-panel au niveau des zones de résidence. La fenêtre
temporelle 2018-2022 couvre une période de transformations importantes des
flux migratoires sénégalais et de leurs effets sur les ménages.

\subsubsection{Construction de l'échantillon}

Notre population cible est l'ensemble des enfants âgés de 0 à 17 ans résidant
dans les ménages enquêtés dans les deux vagues. L'EHCVM I couvre 7\,120 ménages
(vagues 1 et 2 réunies), et l'EHCVM II 7\,120 ménages selon le même plan de
sondage, avec un sous-échantillon panel identifié par la variable \texttt{PanelHH}
\parencite{ansd2019, ansd2022}.

Deux types de pseudo-panel sont utilisés selon la stratégie d'identification :
\begin{enumerate}
  \item Pseudo-panel au niveau des zones de résidence : les ménages des
  deux vagues sont agreg\'es par grappe de sondage (unités primaires), conformément
  à la méthodologie de \textcite{deaton1997}. Ceci est l'approche par défaut
  pour la double différence, dans laquelle les grappes jouent le rôle d'unités
  observées dans le temps.
  \item Panel strictement cylindré (sous-échantillon) : les ménages
  identifiés comme panel dans les deux vagues via \texttt{PanelHH} constituent
  un échantillon de suivi plus rigoureux, qui est utilisé dans les tests de
  robustesse.
\end{enumerate}

Après nettoyage (exclusion des observations avec plus de deux indicateurs de
privation manquants, et des ménages sans information sur les transferts),
l'échantillon final retient les enfants pour lesquels l'intégralité des
indicateurs MODA ainsi que la variable de traitement sont disponibles dans
les deux vagues. La limite principale de cette construction
est l'attrition potentielle entre les deux vagues, qui peut introduire un biais
si les ménages perdus sont systématiquement différents des ménages retenus
-- cette limite est discutée dans la conclusion.

\subsubsection{Variable de traitement}

La variable de traitement $D_i \in \{0, 1\}$ indique si le ménage de l'enfant
$i$ a reçu des transferts d'un membre émigré à l'étranger au cours des 12 mois
précédant l'enquête, conformément aux conventions de la littérature empirique
sur les remittances en Afrique \parencite{azam2016, combes2019}.

La construction de cette variable repose sur les modules de transferts des
deux vagues EHCVM. Dans chaque vague, deux fichiers sont mobilisés :
\texttt{s13a\_1} (identification des ménages ayant reçu au moins un transfert)
et \texttt{s13a\_2} (caractéristiques de chaque transfert et de son expéditeur).
Un ménage est classé comme bénéficiaire ($D=1$) si au moins un transfert reçoit
à un expéditeur dont le lieu de résidence est codé $\geq 4$ dans la nomenclature
EHCVM (variable \texttt{s13aq14} en 2018, \texttt{s13q19} en 2021), les codes
1 à 3 correspondant à des destinations intérieures au Sénégal (même ville, même
région, ailleurs dans le pays). Cette définition exclut les transferts internes
(interrégionaux) qui relèvent d'une logique de redistribution domestique distincte
des envois de la diaspora internationale \parencite{ndiaye2013}.

\subsubsection{Correspondance entre indicateurs et modules EHCVM}

\begin{table}[H]
  \centering
  \caption{Correspondance entre indicateurs MODA et variables EHCVM}
  \label{tab:correspondance}
  \small
  \begin{tabular}{p{2.2cm}p{2.5cm}p{3.5cm}p{3cm}}
    \toprule
    Dimension & Indicateur & Variable EHCVM & Seuil de privation \\
    \midrule
    Assainissement & Type de sanitaire      & \texttt{toilet}                     & $= 0$ (non sain) \\
                   & Partage des toilettes  & \texttt{s05q10}                     & $= 1$ (partage) \\
    Eau            & Source d'eau           & \texttt{eauboi\_ss}, \texttt{eauboi\_sp} & $= 0$ (non améliorée) \\
                   & Temps d'accès          & \texttt{s05q14}                     & $> 30$ minutes \\
    Logement       & Évacuation des ordures & \texttt{ordure}                     & $= 0$ (non sain) \\
                   & Surpeuplement          & \texttt{hhsize} / \texttt{s05q02}   & $> 3$ pers./pièce \\
    Nutrition      & Diversité alimentaire  & \texttt{pcexp} (proxy)              & $< P_{25}$ \\
                   & Sécurité alimentaire   & \texttt{pcexp} (proxy)              & Dép. ali. $< 50\%$ médiane \\
    Santé          & Accès aux soins        & \texttt{mal30j}, \texttt{con30j}    & Malade non-consulté \\
    Protection     & Acte de naissance      & \texttt{s01q19}                     & $\neq 1$ (absent) \\
                   & Travail des enfants    & \texttt{activ7j}                    & Actif (5--14 ans) \\
                   & Vie avec les parents   & \texttt{lien}                       & Lien $\notin \{1\ldots5\}$ \\
    Éducation      & Fréquentation scolaire & \texttt{scol}                       & $= 0$ (5--14 ans) \\
                   & Alphabétisation        & \texttt{alfab}                      & $= 0$ (15--17 ans) \\
    \bottomrule
  \end{tabular}
  \begin{minipage}{\linewidth}
    \small\vspace{2pt}
    \textit{Note :} \texttt{eauboi\_ss}/\texttt{eauboi\_sp} : accès à l'eau potable
    en saison sèche/pluie (1 = amélioré). \texttt{toilet} : type de latrines saines
    (1 = conformes JMP). \texttt{s05q02} : nombre de pièces ; \texttt{s05q10} : partage
    des toilettes ; \texttt{s05q14} : temps d'accès à l'eau (minutes).
  \end{minipage}
\end{table}

\subsubsection{Covariables du modèle de propension}

Le modèle probit estime la probabilité qu'un ménage reçoive des transferts
en fonction de ses caractéristiques pré-transfert observées. Les covariables
sont choisies pour satisfaire l'hypothèse d'indépendance conditionnelle (CIA)
tout en évitant d'inclure des variables potentiellement affectées par le traitement.

\begin{table}[H]
  \centering
  \caption{Covariables du modèle de propension et sources EHCVM}
  \label{tab:covariables}
  \small
  \begin{tabular}{p{2.2cm}p{3.5cm}p{2.8cm}p{2.5cm}}
    \toprule
    Groupe & Variable & Fichier EHCVM & Variable EHCVM \\
    \midrule
    \multirow{4}{2.2cm}{Chef de ménage}
      & Âge du CM            & \texttt{ehcvm\_welfare} & \texttt{hage} \\
      & Sexe (féminin = 1)   & \texttt{ehcvm\_welfare} & \texttt{hgender} \\
      & Niveau d'éducation   & \texttt{ehcvm\_welfare} & \texttt{heduc} \\
      & Statut matrimonial   & \texttt{ehcvm\_welfare} & \texttt{hmstat} \\
    \midrule
    \multirow{2}{2.2cm}{Composition}
      & Taille du ménage     & \texttt{ehcvm\_welfare} & \texttt{hhsize} \\
      & Bien-être initial    & \texttt{ehcvm\_welfare} & \texttt{pcexp} \\
    \midrule
    \multirow{2}{2.2cm}{Localisation}
      & Milieu (urbain = 1)  & \texttt{ehcvm\_welfare} & \texttt{milieu} \\
      & Région (14 régions)  & \texttt{ehcvm\_welfare} & \texttt{region} \\
    \bottomrule
  \end{tabular}
  \begin{minipage}{\linewidth}
    \small\vspace{2pt}
    \textit{Note :} CM = chef de ménage. \texttt{pcexp} est transformée en
    logarithme (\texttt{log\_pcexp}) pour réduire l'asymétrie de la distribution.
    \texttt{region} est traitée comme un facteur à 14 modalités.
    La spécification finale est : $D \sim \texttt{hhsize} + \log(\texttt{pcexp})
    + \texttt{milieu} + \texttt{region} + \texttt{hgender} + \texttt{hage}
    + \texttt{heduc} + \texttt{hmstat}$.
  \end{minipage}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Mesure de la pauvreté multidimensionnelle des enfants}
% ─────────────────────────────────────────────────────────────────────────────

\subsubsection{Fondements conceptuels}

Les mesures monétaires de la pauvreté ne rendent pas compte des conditions
de vie des enfants pour deux raisons fondamentales. D'une part, elles supposent
une distribution intrafamiliale équitable des ressources, ce que les données
infirment généralement \parencite{deaton1997}. D'autre part, elles ignorent
des dimensions non monétaires -- éducation, santé, eau, assainissement,
information -- irréductibles au revenu du ménage et déterminantes pour le
développement de l'enfant \parencite{gordon2003, sen1999}. L'approche par
les capabilités de \textcite{sen1985} fournit le socle conceptuel : ce qui
importe n'est pas seulement les ressources mais la capacité effective de
l'enfant à fonctionner dans chaque dimension fondamentale de son
développement.

\subsubsection{MODA (Multiple Overlapping Deprivation Analysis)}

\paragraph{Spécificités de l'approche MODA.}

L'approche MODA, développée par \textcite{deneubourg2012} au sein du bureau
de recherche de l'UNICEF et appliquée en Afrique subsaharienne par
\textcite{demilliano2014}, part du constat que l'approche Alkire-Foster
présente deux limites lorsqu'elle est appliquée aux enfants. D'abord, elle
utilise les mêmes indicateurs et seuils quel que soit l'âge, alors que les
besoins fondamentaux varient considérablement entre un nourrisson et un
adolescent. Ensuite, les poids et le seuil inter-dimensionnel sont fixés de
manière normative plutôt que dérivés des configurations de privations
effectives.

MODA répond à ces limites selon trois principes. Premièrement, l'enfant
individuel -- et non le ménage -- est l'unité d'analyse. Deuxièmement, les
indicateurs sont désagrégés par groupe d'âge pour refléter les droits
spécifiques à chaque stade de développement. Troisièmement, l'analyse
porte sur les \textit{privations superposées} (overlapping deprivations) :
quelles privations se combinent chez les enfants les plus vulnérables, et
avec quelle intensité.

MODA s'inscrit dans la structure à double seuil d'Alkire-Foster -- seuil
individuel par indicateur, puis seuil inter-dimensionnel $k$ -- dont elle
généralise la logique d'identification en l'adaptant aux droits spécifiques
de chaque groupe d'âge. Elle constitue la variable de résultat principale
de l'estimation PSM-DD présentée dans la section suivante.

\paragraph{Groupes d'âge et dimensions retenus.}

Suivant \textcite{deneubourg2012} et l'adaptation nationale N-MODA développée
pour le Sénégal lors d'un atelier participatif rassemblant l'ANSD et les
ministères sectoriels \parencite{ansd2024}, nous distinguons trois groupes
d'âge avec des indicateurs adaptés aux droits spécifiques de chaque phase de
l'enfance. Le tableau~\ref{tab:moda_indicateurs} présente les dimensions et
indicateurs retenus, en cohérence avec l'EHCVM et la méthodologie N-MODA.

\begin{table}[H]
  \centering
  \caption{Dimensions, indicateurs et seuils de privation -- approche MODA}
  \label{tab:moda_indicateurs}
  \begin{threeparttable}
  \small
  \begin{tabular}{p{1.4cm}p{2.0cm}p{4.8cm}p{1.2cm}p{1.2cm}p{1.2cm}}
    \toprule
    Dimension & Indicateur & Seuil de privation & 0-4 & 5-14 & 15-17 \\
    \midrule
    Assainiss. & Type sanitaire   & \texttt{toilet}=0 (non sain)                        & \checkmark & \checkmark & \checkmark \\
               & Partage toilette & \texttt{s05q10}=1 (partage)                         & \checkmark & \checkmark & \checkmark \\
    \midrule
    Eau        & Source           & \texttt{eauboi\_ss}/\texttt{sp}=0 (non améliorée)   & \checkmark & \checkmark & \checkmark \\
               & Temps accès      & \texttt{s05q14}$>30$ min                            & \checkmark & \checkmark & \checkmark \\
    \midrule
    Logement   & Ordures          & \texttt{ordure}=0 (non sain)                        & \checkmark & \checkmark & \checkmark \\
               & Surpeuplement    & \texttt{hhsize}/\texttt{s05q02}$>3$ pers/pièce      & \checkmark & \checkmark & \checkmark \\
    \midrule
    Nutrition  & Sécurité alim.   & Dép. alimentaires $<50\%$ médiane (\texttt{pcexp})  & \checkmark & \checkmark & \checkmark \\
               & Diversité alim.  & \texttt{pcexp}$<P_{25}$ (proxy)                    &            & \checkmark & \checkmark \\
    \midrule
    Santé      & Accès soins      & Malade non consulté (\texttt{mal30j=1, con30j=0})   & \checkmark & \checkmark & \checkmark \\
    \midrule
    Protection & Acte naissance   & \texttt{s01q19}$\neq 1$ (absent)                   & \checkmark & \checkmark &            \\
               & Travail enfant   & \texttt{activ7j}$\in\{1,2,3,4\}$ (actif)           &            & \checkmark &            \\
               & Vie parents      & \texttt{lien}$\notin\{1\ldots5\}$                  & \checkmark & \checkmark & \checkmark \\
    \midrule
    Éducation  & Scolarisation    & \texttt{scol}=0 (non scolarisé)                     &            & \checkmark &            \\
               & Alphabétisation  & \texttt{alfab}=0 (non alphabétisé)                  &            &            & \checkmark \\
    \midrule
    \multicolumn{3}{l}{\textit{Nombre d'indicateurs par groupe}} & 10 & 13 & 11 \\
    \bottomrule
  \end{tabular}
  \begin{tablenotes}
    \small
    \item \checkmark = indicateur actif pour ce groupe d'âge. Seuil de pauvreté MODA : $k \geq 2$ privations
    simultanées, conforme à la convention de base \parencite{deneubourg2012}.
  \end{tablenotes}
  \end{threeparttable}
\end{table}

\paragraph{Mesures MODA.}

Pour chaque enfant $i$ du groupe $g$ et chaque dimension $j \in \mathcal{D}_g$,
la variable de privation élémentaire est $d_{ij}^g = \mathbf{1}[y_{ij} <
z_j^g]$ et le score de privation totale est $s_i^g = \sum_{j \in \mathcal{D}_g}
d_{ij}^g$. Trois mesures synthétiques sont dérivées.

Le taux de privation par dimension mesure la prévalence de chaque
privation :
\begin{equation}
  H_j^g = \frac{1}{n_g} \sum_{i=1}^{n_g} d_{ij}^g.
  \label{eq:hj}
\end{equation}

Le taux de privation multiple pour un seuil $k$ mesure la proportion
d'enfants souffrant d'au moins $k$ privations simultanées :
\begin{equation}
  P_k^g = \frac{1}{n_g} \sum_{i=1}^{n_g} \mathbf{1}\left[s_i^g \geq k\right].
  \label{eq:pk}
\end{equation}
Nous retenons $k = 2$ comme seuil principal de pauvreté multidimensionnelle
MODA, en accord avec la convention de base de la méthode \parencite{deneubourg2012}.

L'intensité moyenne des privations parmi les enfants privés est :
\begin{equation}
  \bar{s}^g = \frac{\sum_{i:\,s_i^g \geq 1} s_i^g}{\sum_i \mathbf{1}[s_i^g \geq 1]}.
  \label{eq:intensite_moda}
\end{equation}

\subsubsection{Variables de résultat dans l'estimation PSM-DD}

L'approche MODA fournit deux variables de résultat pour le cadre PSM-DD.
La mesure principale est le taux de privation multiple $P_2^g$ (proportion
d'enfants présentant au moins deux privations simultanées) estimé par groupe
d'âge $g$. Les mesures secondaires sont les taux dimensionnels $H_j^g$, qui
permettent d'identifier quelles dimensions contribuent le plus aux effets
estimés.

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Stratégie d'identification causale : PSM-Double différence}
% ─────────────────────────────────────────────────────────────────────────────

\subsubsection{Le cadre des résultats potentiels}

Pour chaque enfant $i$, on définit deux résultats potentiels : $Y_i(1)$ la
mesure de pauvreté MODA si le ménage reçoit des transferts, et
$Y_i(0)$ si le ménage n'en reçoit pas. On observe seulement $Y_i = D_i \cdot
Y_i(1) + (1 - D_i) \cdot Y_i(0)$. Le paramètre d'intérêt est l'effet moyen
du traitement sur les traités (ATT) :
\begin{equation}
  \tau_{ATT} = \E\left[Y_i(1) - Y_i(0) \mid D_i = 1\right].
  \label{eq:att}
\end{equation}
L'estimateur naïf est biaisé car les ménages bénéficiaires et non-bénéficiaires
diffèrent systématiquement sur des caractéristiques observables et inobservables
(biais de sélection).

\subsubsection{L'appariement par score de propension (PSM)}

Le PSM repose sur deux hypothèses. La première est l'indépendance
conditionnelle (CIA) : conditionnellement aux covariables observées $X_i$,
les résultats potentiels sont indépendants du traitement,
\begin{equation}
  \left(Y_i(0),\, Y_i(1)\right) \perp D_i \mid X_i.
  \label{eq:cia}
\end{equation}
La seconde est le support commun : $0 < P(D_i = 1 \mid X_i) < 1$,
garantissant que l'appariement est réalisable pour chaque traité.
\textcite{rosenbaum1983} ont montré que si la CIA est satisfaite pour $X_i$,
elle l'est aussi pour le score de propension $p(X_i) = P(D_i = 1 \mid X_i)$,
estimé par un modèle probit :
\begin{equation}
  p(X_i) = \Phi\left(X_i'\hat{\beta}\right).
  \label{eq:probit}
\end{equation}

Trois algorithmes d'appariement sont mis en œuvre et comparés. L'appariement
aux $k$ plus proches voisins ($k = 4$, avec remplacement) associe à chaque
traité les 4 témoins les plus proches en termes de score :
\begin{equation}
  \hat{\tau}_{k\text{-NN}} = \frac{1}{n_T} \sum_{i \in \mathcal{T}}
  \left[Y_i - \frac{1}{k}\sum_{j \in \mathcal{N}(i)} Y_j\right].
  \label{eq:knn}
\end{equation}
L'appariement par noyau gaussien utilise une moyenne pondérée de l'ensemble
des témoins, les poids décroissant avec la distance en score :
\begin{equation}
  \hat{\tau}_{\text{kernel}} = \frac{1}{n_T} \sum_{i \in \mathcal{T}}
  \left[Y_i - \frac{\sum_{j \in \mathcal{C}} K\!\left(\frac{p_i-p_j}{h}
  \right) Y_j}{\sum_{j \in \mathcal{C}} K\!\left(\frac{p_i-p_j}{h}\right)}
  \right],
  \label{eq:kernel}
\end{equation}
où $h$ est la largeur de bande optimisée par validation croisée. L'appariement
par rayon ($\epsilon = 0{,}05$) exclut les appariements de faible qualité.

La qualité de l'appariement est évaluée par le biais standardisé
\parencite{rosenbaum1983},
\begin{equation}
  SB_j = 100 \cdot \frac{\bar{X}_{1j} - \bar{X}_{0j}}
  {\sqrt{0{,}5\left(V_{1j} + V_{0j}\right)}},
  \label{eq:sb}
\end{equation}
un $|SB| < 10$ indiquant un équilibre satisfaisant \parencite{caliendo2008},
ainsi que par le test $t$ d'égalité des moyennes et la statistique pseudo-$R^2$.

\subsubsection{La double différence (DD)}

La DD exploite la dimension temporelle des deux vagues EHCVM pour contrôler
les effets fixes inobservables stables dans le temps :
\begin{equation}
  \hat{\tau}_{DD} = \left(\bar{Y}^1_{t=2} - \bar{Y}^1_{t=1}\right)
  - \left(\bar{Y}^0_{t=2} - \bar{Y}^0_{t=1}\right),
  \label{eq:dd}
\end{equation}
sous l'hypothèse de \textit{tendances parallèles} :
\begin{equation}
  \E\!\left[Y_i(0)_{t=2} - Y_i(0)_{t=1} \mid D_i = 1\right]
  = \E\!\left[Y_i(0)_{t=2} - Y_i(0)_{t=1} \mid D_i = 0\right].
  \label{eq:parallel}
\end{equation}

\subsubsection{L'estimateur PSM-DD}

La combinaison PSM-DD proposée par \textcite{heckman1997} et
\textcite{heckman1998} neutralise simultanément le biais lié aux différences
observables (PSM) et celui lié aux effets fixes inobservables constants dans
le temps (DD) :
\begin{equation}
  \hat{\tau}_{PSM\text{-}DD} = \frac{1}{n_T} \sum_{i \in \mathcal{T}}
  \left[\Delta Y_i - \sum_{j \in \mathcal{C}(i)} w_{ij} \cdot \Delta Y_j
  \right],
  \label{eq:psm-dd}
\end{equation}
où $\Delta Y_i = Y_{i,t=2} - Y_{i,t=1}$ est la variation intra-individuelle
de la variable de résultat MODA. Les écarts-types sont calculés par
bootstrap (1\,000 réplications) \parencite{caliendo2008}.

\subsubsection{Analyses d'hétérogénéité et de robustesse}

Les analyses d'hétérogénéité estiment l'effet PSM-DD séparément selon le
milieu de résidence (urbain et rural), le groupe d'âge (pour les mesures MODA),
le genre et chaque dimension de privation. La robustesse est vérifiée par la
comparaison entre les trois algorithmes d'appariement, le test des bornes de
Rosenbaum \parencite{rosenbaum2002} et un test placebo sur des dimensions
non affectables par les transferts.
